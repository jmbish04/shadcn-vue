{
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "pnpm -C packages/worker dev",
    "build": "pnpm -C packages/ui-nuxt build && pnpm -C packages/worker build",
    "deploy": "pnpm -C packages/ui-nuxt build && pnpm -C packages/worker deploy",
    "lint": "eslint .",
    "default-branch": "git remote show origin | sed -n '/HEAD branch/s/.*: //p'"
  }
}
